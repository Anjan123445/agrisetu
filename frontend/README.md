# frontend

Owner: Person 1.

Not built yet — this is a placeholder so the repo structure matches
what the team agreed on. Point it at:

- Live backend: see `../backend/README.md` for the local dev URL
  (`http://localhost:8080`) or the deployed Cloud Run URL once
  `../backend/deploy.sh` has been run.
- Fixtures: `../mock-data/` has fake-but-contract-accurate JSON for
  every endpoint if you want to build UI before the backend is ready
  or reachable.
